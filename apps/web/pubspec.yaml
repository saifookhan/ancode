name: ancode_web
description: ANCODE web app and admin dashboard
publish_to: 'none'
version: 1.0.0
environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  shared:
    path: ../../packages/shared
  supabase_flutter: ^2.3.0
  provider: ^6.1.1
  flutter_dotenv: ^5.1.0
  http: ^1.2.0
  url_launcher: ^6.2.1
  printing: ^5.11.0
  pdf: ^3.10.7

flutter:
  uses-material-design: true
  assets:
    - assets/
