name: ancode_mobile
description: ANCODE mobile app (iOS + Android)
publish_to: 'none'
version: 1.0.1
environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  shared:
    path: ../../packages/shared
  supabase_flutter: ^2.3.0
  provider: ^6.1.1
  flutter_dotenv: ^5.1.0
  url_launcher: ^6.2.1
  qr_flutter: ^4.1.0
  printing: ^5.14.2
  pdf: ^3.10.7
  share_plus: ^11.1.0

flutter:
  uses-material-design: true
  assets:
    - assets/
