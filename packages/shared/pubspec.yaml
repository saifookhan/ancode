name: shared
description: Shared models, API clients, validators, and UI kit for ANCODE
version: 1.0.0
environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.3.0
  equatable: ^2.0.5
  intl: ^0.18.1
  qr_flutter: ^4.1.0
  url_launcher: ^6.2.1
  share_plus: ^7.2.1
