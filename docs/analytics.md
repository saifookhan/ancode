# Analytics

## Clicks
`clicks` table stores ancode_id, timestamp, referrer. Increment `ancodes.click_count` on resolve (via Edge Function or trigger).

## Future
- Approximate geo (if available)
- Dashboard charts
- Export
